// test/test.ts
import test from 'ava';

// Define your test cases using the `test` function
test('example test', t => {
    const actual = 1 + 2;
    const expected = 3;
    t.is(actual, expected);
});